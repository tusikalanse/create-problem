#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    assert(0 <= a);
    assert(0 <= b);
    assert(a <= 1000000);
    assert(b <= 1000000);
    return 0;
}